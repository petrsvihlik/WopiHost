{
  "name": "C# (.NET 9)",
  "build": {
    "dockerfile": "./Dockerfile",
    "context": "."
  },
  "features": {},
  "postCreateCommand": "dotnet --list-sdks",
  "containerEnv": {
    "COBALT_PACKAGES_TOKEN": "${{ secrets.COBALT_PACKAGES_TOKEN }}"
  }
}