{
  "_comment": "Keycloak. Replace {host}, {realm}, and the role mapper output as appropriate. Keycloak typically emits realm roles in the 'realm_access.roles' nested claim — flatten via a token mapper or use 'groups' with a groups-membership mapper.",
  "Oidc": {
    "Authority": "https://{host}/realms/{realm}",
    "ClientId": "<keycloak client id>",
    "ClientSecret": "<keycloak client secret>",
    "Scopes": [ "openid", "profile", "email" ],
    "UsePkce": true,
    "RequireHttpsMetadata": true,
    "RoleClaimType": "roles"
  }
}
