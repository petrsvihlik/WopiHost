{
  "_comment": "Auth0. Replace {your-tenant} with your Auth0 tenant subdomain. Auth0 emits roles via a custom claim namespaced rule; configure RoleClaimType to match (e.g. https://yourapp.example.com/roles).",
  "Oidc": {
    "Authority": "https://{your-tenant}.auth0.com/",
    "ClientId": "<application client id>",
    "ClientSecret": "<application client secret>",
    "Scopes": [ "openid", "profile", "email" ],
    "UsePkce": true,
    "RequireHttpsMetadata": true,
    "RoleClaimType": "https://your-app.example.com/roles"
  }
}
